apstr = { "\"" }

colorvalue = { apstr? ~ "#" ~ ASCII_HEX_DIGIT+ ~ apstr? }

face = { ASCII_ALPHA+}

colordef = { face ~ "=" ~ colorvalue }

comment = _{ "//" ~ ANY}

file = {
    SOI ~
    (colordef | comment)+ ~
    EOI
}

