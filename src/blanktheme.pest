apstr = { "\"" }

colorvalue = { "\"" ~ "#" ~ ASCII_HEX_DIGIT+ ~ apstr }

face = { ASCII_ALPHA+}

colordef = { face ~ "=" ~ colorvalue ~ "\n"}

comment = _{ "//" ~ ANY}

file = {
    SOI ~
    (colordef | comment)* ~
    EOI
}

