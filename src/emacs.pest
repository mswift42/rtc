ws = _{ " " }

COMMENT = _{ "//" ~ ANY }

char = { ASCII_ALPHANUMERIC | "." | "_" }

colorvalue = { "#" ~ ASCII_HEX_DIGIT+ }

bg = { ":foreground" }

fg = { ":background" }

themenamedec = { "(" ~ "deftheme" ~ WHITE_SPACE ~ name ~ ")"}

name = { char+ }

foreground = { ANY+ ~ "default" ~ ANY ~ fg ~ WHITE_SPACE ~ fgcol}

background = { ANY+ ~ "default" ~ ANY ~ bg ~ WHITE_SPACE ~ bgcol}

fgcol = {